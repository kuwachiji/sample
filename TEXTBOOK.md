# 地図にピンを刺す — WEB プログラミング・はじめの一歩

たった2つの数字で、地球上の1点が決まります。
その数字をプログラムに渡すと、地図の上にピンが立ちます。
この教材で、そのしくみと書きかえかたを覚えましょう。

| | |
| --- | --- |
| 使う道具 | Leaflet 1.9.4 |
| 地図データ | OpenStreetMap |
| ファイル | `index.html` 1つだけ |
| 所要時間 | 45〜90分 |

---

## 01 サンプルを動かす

`index.html` をダブルクリックすると、東京駅あたりの地図に3本のピンが立っています。
もし地図が「Access blocked」という絵で埋まったら、かわりに `index-gsi.html` を
ダブルクリックしてください（地図が地理院地図に変わります）。
どちらも開けないときは [README.md](README.md) を見てください。
ピンをクリックすると名前の吹き出しが出ます。
下の入力欄に数字を入れて「ピンを刺す」を押すと、その場所へ地図が飛んでピンが増えます。

開きかたは [README.md](README.md) を見てください。

### 座標の調べかた

Google マップで場所を **右クリック** すると、いちばん上に `35.658581, 139.745433` のような
数字が出ます。クリックするとコピーできます。前が緯度、後ろが経度です。

まずは自分の学校を調べてみましょう。

---

## 02 コードを読む

ピンを立てている部分は、たった3行です。

```js
// (1) ピンを1本つくる。[緯度, 経度] の順番
L.marker([35.681236, 139.767125])
  .addTo(map)              // (2) 地図にのせる
  .bindPopup("東京駅");     // (3) 吹き出しをつける
```

サンプルでは、この3行を1か所ずつ手書きするかわりに、住所録のような **リスト** を作って、
上から順に自動で立てています。ここを書きかえるのが今回の作業です。

```js
const pins = [
  { name: "東京駅",           lat: 35.681236, lng: 139.767125 },
  { name: "東京スカイツリー", lat: 35.710063, lng: 139.810700 },
  { name: "皇居",             lat: 35.685175, lng: 139.752799 },
];
```

### 地図の絵はどこから来るのか

地図の絵は、**タイル**と呼ばれる小さな正方形の画像を、何枚も並べて作られています。
どこからタイルを取ってくるかは、次の1行の URL で決まります。

```js
L.tileLayer('https://tile.openstreetmap.org/{z}/{x}/{y}.png', { ... }).addTo(map);
```

`{z}` は拡大率、`{x}` `{y}` はタイルの位置。Leaflet が自動で数字を入れて、
必要な枚数ぶんを取り寄せています。

このサンプルには、この URL だけが違う2つのファイルが入っています。

| ファイル | 地図 | 特徴 |
| --- | --- | --- |
| `index.html` | OpenStreetMap | 世界中どこでも見られる。地名は現地の言葉 |
| `index-gsi.html` | 地理院地図（国土地理院） | 日本だけ。地名が日本語で読みやすく、ダブルクリックでも開ける |

見くらべてみましょう。同じピンでも、地図が変わると印象がずいぶん変わります。

### プログラムの言葉と日本語の対応

| 英語 | 意味 |
| --- | --- |
| `lat` | 緯度。latitude（ラティチュード）の略 |
| `lng` | 経度。longitude（ロンギチュード）の略 |
| `marker` | ピンそのもの |
| `popup` | クリックしたとき出る吹き出し |
| `setView` | 地図の見ている場所と拡大率を決める |
| `zoom` | 拡大率。0 が地球全体、13 で街、18 で建物が見える |
| `tileLayer` | 地図の絵そのもの。小さな四角い画像を並べて作られている |

> コードを書きかえたら、**保存** して、ブラウザを **再読み込み**。
> この2つを忘れると画面は変わりません。

---

## 03 やってみよう

### 課題 1 — 自分の学校にピンを刺す 〔やさしい〕

Google マップで学校の座標を調べ、画面の入力欄に打ち込んでピンを立ててください。
立てられたら、今度は `pins` のリストに書き足して、最初から表示されるようにしましょう。

> **ヒント** リストに足すときは、前の行の `},` のあとで改行します。
> 行の終わりの **カンマ** を忘れると動かなくなります。

### 課題 2 — 「わたしの好きな場所マップ」を作る 〔ふつう〕

行ったことのある場所、行ってみたい場所を5つ選んで `pins` に入れてください。
全部のピンが最初から画面に入るように、`setView` の座標とズームも調整しましょう。

> **ヒント** 全国に散らばるなら `setView([36.5, 138.0], 5)` あたりが日本全体。
> 名前に「行きたい理由」を書いておくと、吹き出しがそのまま紹介文になります。

### 課題 3 — ピンとピンを線でつなぐ 〔ちょっと難しい〕

旅行のルートのように、2つ以上の地点を線で結んでみましょう。
ピンと同じく、座標を並べて `addTo(map)` するだけです。

```js
L.polyline([
  [35.681236, 139.767125],  // 東京駅
  [34.985849, 135.758767],  // 京都駅
], { color: "red" }).addTo(map);
```

> **ヒント** `L.circle([緯度, 経度], { radius: 500 })` で半径500mの円も描けます。
> 「学校から半径1km」を描いてみるとおもしろいです。

### 課題 4 — クリックした場所の座標を表示する 〔挑戦〕

地図をクリックしたとき、そこの緯度経度を吹き出しで出してみましょう。
「クリックされたら〇〇する」という書きかたを覚えると、できることが一気に増えます。

```js
map.on("click", function (e) {
  // e.latlng の中に、クリックした地点の座標が入っている
  L.popup()
    .setLatLng(e.latlng)
    .setContent(e.latlng.lat + ", " + e.latlng.lng)
    .openOn(map);
});
```

> **ヒント** 小数がやたら長く出ます。`e.latlng.lat.toFixed(6)` と書くと小数第6位までに揃えられます。

---

## 04 こまったときは

| こうなった | たいてい、これが原因 |
| --- | --- |
| 地図が真っ白 | CSS の `#map { height: 60vh; }` が消えている。地図を入れる箱には高さの指定が必要です |
| 地図が「Access blocked」「403」の絵で埋まる | `index.html` をダブルクリックで開くと起こります。`index-gsi.html` のほうを開いてください |
| 地図の絵が出ず、灰色のまま | インターネットにつながっていない。地図はネットから取り寄せています |
| ピンが海の上や外国に立つ | 緯度と経度が逆。日本なら緯度が 35 前後、経度が 139 前後です |
| 書きかえたら何も出なくなった | カンマか閉じカッコの書き忘れ。1文字でも足りないと止まります。直前の変更を戻してみましょう |
| 保存したのに変わらない | ブラウザの再読み込み忘れ。それでもダメなら Shift を押しながら再読み込み |

原因がわからないときは、ブラウザの **デベロッパーツール**（Windows は `F12`、Mac は `Command + Option + I`）を
開き、Console のタブを見てください。赤い文字でエラーが出ていたら、それをそのまま検索すると答えが見つかります。

---

## 05 もっと調べる

| サイト | 何のために見るか |
| --- | --- |
| [Leaflet — Quick Start Guide](https://leafletjs.com/examples/quick-start/) | 今日やったことの公式版。英語ですが、コードを目で追うだけでも参考になります |
| [Leaflet — ドキュメント](https://leafletjs.com/reference.html) | できることの全一覧。`Marker` や `Polyline` を探すと、指定できる色や大きさが載っています |
| [MDN — JavaScript（日本語）](https://developer.mozilla.org/ja/docs/Web/JavaScript) | 言葉の意味を調べるならここ。`const` や `forEach` など、わからない単語を引く辞書として使います |
| [MDN — ウェブ開発を学ぶ](https://developer.mozilla.org/ja/docs/Learn_web_development) | HTML・CSS・JavaScript を最初から順番に学びたくなったら |
| [地理院地図（国土地理院）](https://maps.gsi.go.jp/) | 日本の公式の地図。標高や昔の空中写真も見られます。座標の確認にも使えます |
| [OpenStreetMap](https://www.openstreetmap.org/) | このサンプルが使っている地図の本体。世界中のボランティアが作っている、誰でも使える地図です |

---

地図データ © OpenStreetMap contributors（[ODbL](https://www.openstreetmap.org/copyright)） /
[地理院タイル](https://maps.gsi.go.jp/development/ichiran.html)（国土地理院）。
作った地図を公開するときは、この著作権表示を必ず残してください。地図を使わせてもらうときの約束です。
